/*we will create a function called discoverDiametro, where we will receive
a parameter called radius and a parameter called diameter.
Inside the function, we will return the result of its diameter.*/

function descobreDiametro(raio, diametro) {
  return raio * diametro * Math.PI * radius;
}
console.log(Math.PI);
